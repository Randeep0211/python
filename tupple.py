#### A tuple is an ordered, immutable collection of elements. Think of it like a list, 
# but once created, it cannot be changed—no adding, removing, or modifying elements.

ec2_instance = ("instance_1","instance_2",True,1)
# print(len(ec2_instance))
# ec2_instance.pop() 
